<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.certificates">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

         <activity android:name=".MainActivity">
         <intent-filter>
         <action android:name="android.intent.action.MAIN" />


         <category android:name="android.intent.category.LAUNCHER" />
         </intent-filter>
         </activity>
        <activity android:name=".LawActivity" />
        <activity android:name=".ManageActivity" />
        <activity android:name=".RiskActivity" />
        <activity android:name=".CreditActivity" />
        <activity android:name=".LoanActivity" />
        <activity android:name=".BankActivity" />
        <activity android:name=".LawSectionActivity" />
        <activity android:name=".LawHistoryActivity" />
        <activity android:name=".LawForecastActivity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".FourthActivity" />
        <activity android:name=".FifthActivity" />
        <activity android:name=".LawWriteActivity"/>
        <activity android:name=".LawNoteActivity"></activity>
    </application>

</manifest>